# Implementation Plans

Generated by the improve skill on 2026-07-13. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status | Issue |
|------|-------|----------|--------|------------|--------|-------|
| 001 | Add login/register/logout feature tests | P1 | S | — | DONE | [#1](https://github.com/asgarimehdi/lacms/issues/1) |
| 002 | Guard file upload endpoint with CSRF and rate limiting | P1 | S | — | DONE | [#2](https://github.com/asgarimehdi/lacms/issues/2) |
| 003 | Add rate limiting to login, register, and comment submission | P1 | S | — | DONE | [#3](https://github.com/asgarimehdi/lacms/issues/3) |
| 004 | Replace User model #[Fillable] attribute with $fillable array | P2 | S | — | DONE | [#4](https://github.com/asgarimehdi/lacms/issues/4) |
| 005 | Fix double query in Posts admin Index component | P2 | S | — | DONE | N/A |

Status values: TODO | IN_PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 (auth tests) should land first — it establishes verification baseline for auth changes.
- 002 and 003 are independent of each other and 001, but all three are P1.
- 004 and 005 are P2 and can run in parallel with 001-003 or after.

## Implementation summary

All 5 plans implemented in commit `14fd79f`:
- `tests/Feature/CmsFeatureTest.phi` — 7 new auth tests (login validation, invalid creds, register validation, unique email, password confirmation, logout HTTP). All 33 tests pass.
- `app/Http/Controllers/ImageUploadController.phi` — new controller with file type/size validation. `routes/auth.phi` updated to use it with `throttle:10,1`.
- `routes/auth.phi` — `throttle:5,1` on login and register routes.
- `routes/web.phi` — `throttle:3,1` on blog post route.
- `app/Models/User.phi` — replaced `#[Fillable]` attribute with `protected $fillable` array.
- `app/Livewire/Pages/Posts/Index.phi` — added `$cachedPosts` memoization and `updated()` invalidation.

## Findings considered and rejected

- **Finding 1 (no registration endpoint)**: `Register.phi` is called via Livewire's POST mechanism through the Livewire route handler — no separate HTTP route needed. Not a bug. Test coverage gap (covered by plan 001) is the real finding.
- **Finding 7 (Category no slug auto- generation)**: all admin form implementations auto-generate slugs from the name input. No bug visible in code. Low impact. Not worth a plan at this time.